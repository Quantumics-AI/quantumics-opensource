export interface Certificate {
  auth: boolean;
  user_id: string;
  email: string;
  redash_key: string;
  user: string;
  token: string;
  refreshToken: string;
  userRole: string;
  userImageUrl: string;
  userType: string;
}
