import { AuditFilterPipe } from './audit-filter.pipe';

describe('AuditFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
